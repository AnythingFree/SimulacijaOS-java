package kernel;

import cpu.CPU;

public class Kernel {
	
	CPU cpu = new CPU();
	ProcessScheduler processScheduler = new ProcessScheduler();
	//RAM ram = new RAM();
	
	//Disk disk = new Disc();
	//FileTree fileTree = new FileTree(); 
	
	

}
